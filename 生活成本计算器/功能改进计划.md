# 生活成本计算器 · 功能改进计划

> **定位**：辅助底层劳动者。最有价值的不是"算得更准"，而是
> **"告诉他不知道的权利 / 补贴 / 陷阱"** + **"算完给可立刻执行的动作"**。
> 信息差比收入差更致命——很多人不是不够努力，是不知道该领的钱没领、该避的坑没避。

> **设计转变（2026-06-19）**：工具不必把所有答案都算死——求助渠道、维权方案、
> 地方政策等，问 AI 更详细。但底层劳动者不习惯用 AI、不会把问题问清楚。
> 新分工：**工具负责结构化计算 + 用户数据填充 + 提示词模板，生成「带数据、一键复制」
> 的提示词，让用户粘到任意 AI 去问**。补的是"提问差"。
> **「需联网补数据」的功能（失业金/补贴/各地最低工资精确值）走两条腿**：
> ① 先在网上搜集整理成本地调研数据（`rights_data`/`cost_data`，算硬数字作基准真相，离线可用）；
> ② 同时给出问 AI 的更全面提示词（补当地最新公告、个体差异、申领流程等查不全的部分）。
> ——不是"只外包给 AI、干等数据"，而是"本地数据兜底 + 提示词补全"。
> 详见记忆 `design-shift-prompt-generation`，方法论用本地化的 `prompt-engineering-patterns` skill。

---

## ✅ 已完成

- **借贷真相页**（`pages/page_debt.py` + `calc_engine.compute_loan_apr`）
  - 输入借款本金 / 每月还款 / 期数 → 反算真实年化（IRR 口径）
  - 评级（极高≥36% / 高利贷24~36% / 偏高>15% / 正常）+ 维权提示
  - 已接入导航「⑥ 借贷真相」，冒烟通过

- **债务功能扩展**（2026-06-19）—— 借贷真相页扩为 **Notebook 5 标签页**（`page_debt.py` 重构 +
  `calc_engine` 新增 4 个纯函数 + helper；`compute_loan_apr` 重构复用 helper，对外不变）
  - ① 反算真实年化（原功能保留）
  - ② **还款方式对比** `compare_loan_methods`：等额本息 vs 等本等息（固定手续费），
    点破消费分期「月费率0.7%」真实年化≈名义2倍的陷阱
  - ③ **可承受负债上限** `compute_affordable_debt`：按月结余反算能借多少（50%/30%档），超了警告
  - ④ **多笔债雪球/雪崩** `simulate_debt_payoff`：两策略对比（雪崩更省）、还清顺序、
    **最低还款盖不住利息的失控检测**；含动态增删行（项目首例）
  - ⑤ **以贷养贷螺旋** `simulate_loan_spiral`：利滚利逐月演示、翻倍月数、止血线
  - Notebook 标签样式在 `gui_widgets.apply_style` 统一配置（项目首次引入 Notebook，用户选定）
  - 逻辑层自测全过（`python calc_engine.py` 自检 + 无头实例化测试 5 tab + 边界用例）；UI 已确认（Notebook 标签铁灰配色）

- **劳动权益页**（2026-06-19）—— 新增 `pages/page_rights.py` + 导航「⑦ 劳动权益」
  + `calc_engine.compute_overtime_pay` / `compute_min_wage_check`（法定标准，全国统一，无需各地数据）
  - 加班费依法反算（150%/200%/300% + 12333/劳动仲裁维权）、最低工资对照（低于即违法）
  - 失业金 / 灵活就业社保补贴 / 工伤各地数据：已联网搜集整理进 `rights_data.py`
    （31省最低工资、失业金、4050 补贴、工伤各省差异；详见 `调研数据/07_待补数据清单.md`，已补齐），
    本地算硬数字 + 各卡片配「问 AI」提示词补当地最新公告与申领细节
  - **加班费「生成问 AI 提示词」示范**（`build_overtime_prompt`）：结果区一键生成
    带用户数据（月薪/加班时长/被欠/在职/证据/城市）的提示词，复制去问任意 AI，
    拿结合本地政策的详细建议——验证通过后铺开到其他功能

- **求助渠道页**（2026-06-19，后改为按钮墙）—— `pages/page_help.py` + 导航「⑧ 求助渠道」
  - 改为**场景按钮墙**（10 个常见困境按钮：欠薪 / 社保 / 辞退 / 无合同 / 工伤 / 消费 /
    网贷催收 / 租房 / 派遣中介 / 心理），点击弹窗生成场景化提示词（`build_help_prompt`），
    让 AI 指路找谁、打什么电话、怎么操作；底部兜底提示 12345 / 110 / 120

- **数据持久化（本地存档）**（2026-06-20，P0 #1）—— 打开即恢复上次状态，关闭即自动保存
  - 探索发现：save/load（`profile.py` 的 `save_to_file/load_from_file`）+ 档案页「💾保存/📂加载」按钮**早已实现**；
    本次补的是**自动存载**。`profile.py` 加 `app_data_dir`（程序同级 `data/`，`sys.frozen` 兼容打包 exe）
    + `save_last_profile/load_last_profile`（静默 + 损坏容错）；`page_profile.on_apply` 加 `silent`、抽出 `_push_to_pages`，
    确认档案即存为「上次」；`page_debt` 加 `collect_state/apply_state/save_state/load_state` 覆盖 5 tab（含多笔债清单重建）；
    `gui_app` 加 `_auto_restore`（启动静默载入 + 自动同步处境/对比/三座山/权益）+ `_on_close`（关窗兜底保存）。
  - 范围：档案（自动同步各页）+ 债务页（profile 覆盖不到的手填输入）；处境等页靠 `apply_profile` 同步，无需单独存。
  - 顺手修了 `profile.py` 自检一处 pre-existing 笔误（tier 默认已是「三线」，断言仍写「二线」）。
  - 验证：profile 自检 + 隐藏 Tk 实例往返测试（档案/债务状态/损坏容错）全过；`data/` 已加 `.gitignore`。

- **打包 exe（PyInstaller）**（2026-06-20，P0 #2）—— 单 exe 双击即用，无需装 Python
  - 装了 `pyinstaller 6.21.0`；写 `build.spec`（`--onefile` + `console=False` 不弹黑框 + `name='生活成本计算器'`）。
  - **关键澄清**：原计划写「含 rights_data/cost_data/调研数据 资源」是误解——程序零外部资源（无图片/字体/json 模板），
    `rights_data`/`cost_data` 是 .py 模块经 import 链自动收集，**「调研数据/」是开发文档、运行时不读、不打包**；故 `datas=[]`。
  - 产物 `dist/生活成本计算器.exe`（约 11 MB）；后台启动冒烟——存活 8s+ 未崩（bootloader/python/tkinter 初始化 OK）。
  - 写了 `打包说明.md`（环境/步骤/本地验证/常见问题：杀毒误报·启动慢·SmartScreen）；`.gitignore` 加 `build/`/`dist/`/`*.exe`。
  - 待用户本地双击做完整 GUI 验证（本环境无法可靠看界面）。

- **提示词一键打开 AI 网页**（2026-06-20，P0 #3）—— 复制提示词 + 自动打开 AI 网页
  - `open_prompt_dialog` 操作行在「一键复制」旁加 AI 平台下拉（DeepSeek/豆包/Kimi/通义，**默认 DeepSeek**）
    +「复制并打开」按钮：点一下先把提示词放进剪贴板，再 `webbrowser.open` 打开所选 AI 网页，
    用户粘贴（Ctrl+V）即可提问，省去手动开网页一步。
  - 平台清单集中在常量 `AI_PLATFORMS` / `DEFAULT_AI`（`gui_widgets.py`），改默认或加平台改一处即可。
  - 验证：常量 + 签名 + 弹窗构建（含新控件）全过；实际点按钮打开 DeepSeek 待用户确认。

- **首次档案填写向导**（2026-06-20，P0 #4，原「新手引导」改为向导）—— 分步问答降低建档心理负担
  - 朋友建议：档案页一次性 20+ 字段心理负担重；改成首次启动弹**分步向导**（7 步，相关字段分组），
    字段级 `show_if` 实现「没伴侣/没车/没副业/没孩子就跳过对应后续字段」的智能跳过；
    填完自动 `apply_profile` + `on_apply(silent)` 同步档案页和处境/对比/三座山/权益各模块。
  - 新增 `pages/page_wizard.py`（向导引擎：单 Toplevel 动态内容区、控制字段 trace 重渲染、完成建档/以后再说）；
    `profile.py` 加 `WIZARD_STEPS`；`gui_app` 首次（无 `last_profile`）触发，去掉了原功能映射式新手引导。
  - 触发判断用「无上次档案 = 首次」（复用 #1 的 `load_last_profile`），不再用 guide 标记（已删 `has_seen_guide` 等）。
  - 验证：字段合法性 + 构建 + finish 等效同步（档案页回填 / 城市识别 / 处境页同步）全过；实际交互待用户确认。

- **多子女按年龄段分别计数**（2026-06-20）—— 修"全部子女同段"假设
  - 旧 `child_age_group`（单值 combo）→ 4 段各填数量（`child_baby/kg/school/uni`，组装成 `children_by_age` dict）。
  - `calc_engine` 养娃月成本 + 专项附加扣除改**按段分别累加**（`_normalize_children` 辅助 + 全 0 兜底归中小学）；
    顺带修 `compare_cities` 漏传子女段的旧 bug、给问 AI 提示词补子女段描述。
  - 旧 `last_profile.json` 自动迁移（`child_age_group` 字符串 → 对应段计数，不丢数据）。
  - 范围：处境页/对比页（月度成本）；三座山·养娃页是「养一个孩子 0-22 岁总额」独立前瞻模型，未改。
  - 验证：混合段（1 婴幼儿 + 2 中小学）养娃 = 4400、专项按段、旧档案迁移、compare 传段 全过。

- **反诈骗模块**（2026-06-20，P1 #5）—— 按钮墙按骗局类型 + 特征速查 + 紧急止损
  - 新增 `pages/page_anti_fraud.py`（导航「⑧ 反诈骗」，about 顺延为⑨）：6 类骗局按钮墙
    （刷单返利 / 网贷先交钱 / 杀猪盘 / 冒充客服公检法 / 中奖理赔先交钱 / 其他）→
    弹窗 `build_antifraud_prompt` 让 AI 判断是否骗局 + 紧急止损（别转账/别给验证码/别屏幕共享）+ 报警（96110 / 110 / 国家反诈 APP / 12321）。
  - 附「特征速查」区（不依赖 AI 也能快速识别）+ 兜底止损提示。
  - 顺带把 help + antifraud 加进档案同步 targets（求助/反诈的城市从档案自动带入，修了 help 之前没同步城市的遗漏）。
  - 验证：6 类提示词结构 + App 含反诈页 全过；实际文案/特征待用户审。

- **导航优化 + 求助反诈合并**（2026-06-20）—— 修侧边栏溢出
  - 导航改 `ScrollableFrame`（可垂直滚动）；滚动条**自动隐藏**（内容不超高时藏、超高才显，
    改 `gui_widgets.ScrollableFrame._update_vsb`，全局所有滚动区一并生效）。按钮 padding 恢复 12（曾临时改 6 太挤）。
  - **求助渠道 + 反诈骗合并为「⑦ 求助与反诈」一页**（`pages/page_assist.py`，Notebook 双 tab，仿借贷页）——
    导航从 9 项减到 8 项，进一步缓解溢出；about 顺延⑧；同步 targets 的 help+antifraud → assist。
  - 验证：自动隐藏（不超高藏 / 超高显）+ 8 导航 + assist 双 tab 全过。

- **住房决策（集成城市对比）**（2026-06-20，P1 #7）—— 买 vs 租 / 公积金 / 利率，Notebook 四标签
  - 「③ 城市与住房」（原城市加减法）重构为 Notebook：① 城市对比（原功能不变）+ ② 买 vs 租 + ③ 公积金额度 + ④ 利率压力测试。
  - calc_engine 加 `_monthly_payment`/`_remaining_principal`（等额本息月供/剩余本金 helper）+ `compare_buy_rent` / `housing_fund_loan` / `rate_stress_test` + 3 个 `build_*_prompt`；cost_data 加 `HOUSING_FUND`。
  - **买 vs 租算法（去涨幅假设）**：不预测房价涨跌，按「房价不变」中性估算——买房净成本 = N 年贷款利息（本金经房子按买时价收回）；租房 = 月租×12×N。当前房价普遍下跌，红字强调房价跌时买房还要额外亏跌幅。
  - 顶部**红字警示**「房价波动剧烈，结果为估算，建议用问 AI 结合最新行情」（用户要求，跨 tab 可见）；各 tab 都配「问 AI」提示词查本地精确。
  - 验证：月供公式一致（贷 50 万/30 年/2.85% = 2068）、3 函数合理、Notebook 4 tab 全过。

- **个税优化**（2026-06-20，P1 #9）—— 年终奖单独 vs 合并 + 专项扣除提醒，劳动权益页加 tab
  - 劳动权益页加 tab「⑥ 个税优化」：输入月薪/年终奖/专项扣除/社保 + 家庭情况（子女/赡养/房贷/教育）
    → 年终奖**单独计税 vs 合并计税**对比（给省税建议）+ 专项附加扣除提醒（哪些用足/可能漏报）。
  - cost_data 加 `TAX_BRACKETS_ANNUAL`（年度 7 级累进）+ `calc_annual_income_tax` + `bonus_monthly_rate`；
    calc_engine 加 `bonus_tax_compare` / `special_deduction_hints` / `build_tax_prompt`。
  - 验证：月薪 2 万 / 奖金 3 万 → 单独计税省 5100（单独 900 vs 合并 6000）；年度个税 10 万 = 7480；权益页 6 tab 全过。

- **公式微调 + 本地救助(#10) + 医保就医(#11)**（2026-06-20）—— 依补充调研数据（08/09）
  - **公式微调**：一线灵活就业社保 1880→2050、一线最低工资 2600→2700（调研明确偏低）；生存成本/社保**公式逻辑不动**（均在区间内），仅 ADULT_ANNUAL note 补口径说明。
  - **本地救助(#10)**：新建 `relief_data.py`（223 条低保标准 + 边缘/特困/临时/医疗救助/公租房/财产规则 + `get_dibao_for_city` 三段式查表）；劳动权益页加 tab⑦，`check_relief` 三层对照（符合/边缘/接近/不符）+ 财产提醒 + 问 AI。
  - **医保就医(#11)**：新建 `medical_data.py`（31 省职工住院 + 居民 + 大病 + 异地/慢病/药品/DRG + `get_employee_rate`/`estimate_inpatient`）；独立页 `page_medical.py`（导航⑧，住院估算 + 医保速查两 tab），`estimate_medical_cost`（基本+大病+异地多层）+ 问 AI。
  - 验证：低保查表（深圳→广东/成都 990/南昌精确）、救助三层判断、广州职工 5 万报 39200、9 导航 全过。

- **债务健康仪表盘**（2026-06-20，#14）—— 借贷真相页加 tab⑥
  - 输入总负债/月收入/月可还款/平均年化 → 负债率、月供比、还清月数、风险等级（健康/警戒/危险）。
  - `assess_debt_health`：失控检测（月还 < 月利息 →「还款盖不住利息，越还越多」）+ 还清公式 `n=-ln(1-Pr/M)/ln(1+r)` + `build_debt_health_prompt`。
  - 验证：负债 10 万/月入 8 千 → 危险（负债率 104%/月供 38%/47 月）、失控检测、健康档、借贷页 6 tab 全过。

---

## 📋 待做（基于《迭代方案.md》，按优先级；每项含实现路径）

### 🔴 P0 — 体验 / 分发（门槛低，先做）

**1. 数据持久化（本地存档）** ✅ 已完成（2026-06-20，详见上方「✅ 已完成」区）

**2. 打包 exe（PyInstaller）** ✅ 已完成（2026-06-20，详见上方「✅ 已完成」区）

**3. 提示词一键打开 AI 网页** ✅ 已完成（2026-06-20，详见上方「✅ 已完成」区）

**4. 新手引导 → 首次档案向导** ✅ 已完成（2026-06-20，详见上方「✅ 已完成」区）

### 🟡 P1 — 补信息差新维度（高价值）

**5. 反诈骗模块（新）** ✅ 已完成（2026-06-20，详见上方「✅ 已完成」区）

**6. 副业 / 增收建议** ❌ 不做（2026-06-20）—— 市场动态、极易给出过时/不当建议，工具和 AI 都难靠谱，反而误导，放弃。

**7. 住房决策（买 vs 租 / 公积金）** ✅ 已完成（2026-06-20，详见上方「✅ 已完成」区）

**8. 廉价保险配置** ❌ 不做（2026-06-20）—— 不该推荐保险（风险大、易误导），放弃。

**9. 个税优化** ✅ 已完成（2026-06-20，详见上方「✅ 已完成」区）

**10. 本地救助对照** ✅ 已完成（2026-06-20，详见上方「✅ 已完成」区）

**11. 医保 / 就医** ✅ 已完成（2026-06-20，详见上方「✅ 已完成」区）

### 🟢 P1 — 现有模块加强

- **12. 档案长期跟踪**：多次存档快照，看结余/资产趋势（依赖 #1 持久化）。
- **13. 处境消费结构分析**：恩格尔系数、各类占比、「哪些可砍」建议。
- **14. 债务健康仪表盘** ✅ 已完成（2026-06-20，详见上方「✅ 已完成」区）

### 🔵 P2 — 分发 / 演进（长期）

- **15. 移动端（微信小程序）**：触达（劳动者手机为主）；tkinter→小程序大改写，先验证桌面价值。
- **16. 数据年度更新机制**：`rights_data`/`cost_data` 年初更新流程 + 数据来源清单。
- **17. 单元测试**：`calc_engine` 纯函数 pytest 套件，防回归。
- **18. 生命周期推演**（原待做 #6）：N 年资产/负债轨迹 + 单变量对比。
- **19. 推广**：工会/劳务市场/社区/职介所。

### 贯穿（文案）
- **反鸡汤 / 诚实性**（原待做 #7）：已部分落实（survive_months 口径修正、文案走查），持续——结果区分「个人可改」vs「结构性」，不甩锅个人。

---

## 🛠 技术说明
- **架构**：`calc_engine.py`（纯函数）+ `cost_data.py` / `rights_data.py`（数据）+ `pages/page_xx.py`（tkinter 页）+ `gui_widgets.py`（组件/样式/`open_prompt_dialog`/`readonly_note`）+ `gui_app.py`（导航）。
- **UI**：tkinter 原版；工具集合页（借贷/劳动权益）用 Notebook，其它 ScrollableFrame + CardFrame。
- **提示词体系**：各功能 `build_*_prompt(用户数据)` 纯函数 + `gui_widgets.open_prompt_dialog`（小按钮→弹窗，城市输入 + 一键复制 + 重新生成）。
- 纯计算类照 `compute_loan_apr` 模式；需数据类**先搜集整理进 `rights_data`/`cost_data`（硬数字基准）+ 同时给问 AI 的更全面提示词**（两条腿，见顶部方针）。

---

## ▶️ 推荐先做
- **P0 四件已全部完成**（数据持久化 / 打包 exe / 一键打开 AI / 首次档案向导）——工具已「好用 + 能分发」。
- 接下来 **P1**：反诈骗 ✅ + 住房决策 ✅ 已完成；个税优化（#9）进行中；副业/保险已确认不做。
- 详见《迭代方案.md》。

> 用户偏好：贴合底层劳动者真实处境，强调可执行性 + 诚实性，区别于服务中产的理财 App。
