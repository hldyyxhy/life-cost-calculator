# 翻译清单：Python calc_engine → TypeScript core

> 基于 `calc_engine.py`（2594 行，56 函数 + 5 内嵌闭包）+ 4 数据模块的结构化梳理。
> 本文件是后续批次的施工图。**进度标记**：✅ 已译并交叉验证 / ⏳ 本阶段试点 / ⬜ 待译。
> 详见 `多端迁移方案.md`、本目录 `README.md`。

## 翻译批次（按依赖拓扑）

### 批次 0 — 数据层（前置，所有计算依赖）

**数据常量**：✅ `export_data.py` 已导出为 `src/data/{cost,rights,medical,relief}.json`（含派生常量 CITY_TO_TIER/HOUSE_PURCHASE/CITY_NAMES 运行时值）。

**cost_data 逻辑函数**（⏳ 本阶段译于 `src/data/cost.ts`）：

| Python (cost_data.py) | TS 函数 | 状态 |
|---|---|---|
| `city_factor:485` | `cityFactor` | ⏳ |
| `raise_factor:490` | `raiseFactor` | ⏳ |
| `adjust_by_tier:495` | `adjustByTier` | ⏳ |
| `adjust_by_tier_level:500` | `adjustByTierLevel` | ⏳ |
| `calc_personal_income_tax:505` | `calcPersonalIncomeTax` | ⏳ |
| `calc_annual_income_tax:520` | `calcAnnualIncomeTax` | ⬜（bonus_tax 用） |
| `bonus_monthly_rate:531` | `bonusMonthlyRate` | ⬜（bonus_tax 用） |
| `fmt_money:539` | `fmtMoney` | ⬜ |
| `_equal_installment_ratio:275` | （派生 HOUSE_PURCHASE 已导出，无需重算） | — |

### 批次 1 — 底层工具（叶子，可并行）

| Python (calc_engine.py) | TS 文件/函数 | 状态 | 备注 |
|---|---|---|---|
| `_normalize_children:453` | `calc/normalizeChildren.ts` | ⏳ | 过滤0/null，空+num>0兜底中小学 |
| `_survival_baseline:548` | `calc/survival.ts` | ⏳ | 读 cost.json 多常量 |
| `_money:1929` | `calc/money.ts` | ⬜ | situation 不依赖，留批次3 |
| `_profile_brief:1934` | `calc/profileBrief.ts` | ⬜ | 12个 prompt 复用，留批次4 |
| `_solve_monthly_irr:748` | `calc/irr.ts` | ⬜ | 二分法200次，**数值敏感** |
| `_annual_irr_from_monthly:774` | 同上 | ⬜ | |
| `_level_from_annual_irr:779` | 同上 | ⬜ | |
| `_monthly_payment:850` | `calc/loan.ts` | ⬜ | 等额本息，**数值敏感** |
| `_remaining_principal:858` | 同上 | ⬜ | **数值敏感** |
| 模块常量 `CARE_MODE_MAP/MONTH_WORK_DAYS_PAY/DAILY_HOURS/HELP_SCENARIOS/FRAUD_TYPES` | `calc/constants.ts` | ⬜ | |

> ⚠️ **前置声明陷阱**：`_money`/`_profile_brief` 定义在 L1929/L1934 却被 L679 compare_cities 和 12 个 build_*_prompt 调用。Python 靠运行时解析，TS 必须用 `export function`（声明提升）或前置 import。

### 批次 2 — 独立计算（依赖批次1 + cost_data）

`compute_life_cost:30` / `_estimate_target_wage:575` / `compute_family_situation:602` / `compute_risk_indicators:647` / `compute_loan_apr:790` / `compute_affordable_debt:947` / `simulate_debt_payoff:1005`（**数值敏感**：0.005容差+free_pool滚动）/ `simulate_loan_spiral:1160`（**数值敏感**）/ `compute_overtime_pay:1243` / `compute_min_wage_check:1298` / `assess_overtime_claim:1346` / `compare_buy_rent:1987` / `housing_fund_loan:2067` / `rate_stress_test:2106` / `bonus_tax_compare:2199`（**数值敏感**，依赖3个税表函数）/ `special_deduction_hints:2245` / `assess_debt_health:2415`（**数值敏感**：math.log）。⬜ 全部待译。

### 批次 3 — 组合计算（依赖批次2）

| Python | TS | 状态 |
|---|---|---|
| `_build_interpretation:461` | `calc/interpretation.ts buildSituationInterpretation` | ⏳ 本阶段 |
| `compute_current_situation:252` | `calc/situation.ts computeCurrentSituation` | ⏳ 本阶段（试点核心） |
| `compute_surplus:589` | — | ⬜ |
| `compare_cities:679` | — | ⬜ 依赖 `_money` |
| `compare_loan_methods:869` | — | ⬜ 依赖 IRR/月供 |

### 批次 4 — 提示词生成（16个 build_*_prompt，纯字符串拼接，零数值风险）

`build_overtime_prompt:1431` / `build_loan_apr_prompt:1482` / `build_compare_methods_prompt:1504` / `build_affordable_debt_prompt:1526` / `build_debt_payoff_prompt:1549` / `build_spiral_prompt:1568` / `build_min_wage_prompt:1589` / `build_unemployment_prompt:1611` / `build_subsidy_prompt:1644` / `build_help_prompt:1704` / `build_antifraud_prompt:1769` / `build_current_situation_prompt:1798` / `build_milestones_prompt:1849` / `build_compare_prompt:1873` / `build_injury_prompt:1899` / `build_buy_rent_prompt:2138` / `build_fund_prompt:2157` / `build_rate_stress_prompt:2176` / `build_tax_prompt:2271` / `build_assistance_prompt:2361` / `build_medical_prompt:2391` / `build_debt_health_prompt:2464`。⬜ 全部待译，依赖 `_profile_brief`。

### 批次 5 — 外部薄封装

`check_relief:2311`（依赖 relief_data + rights_data.CITY_TO_PROVINCE）/ `estimate_medical_cost:2385`（薄封装 medical_data.estimate_inpatient）。⬜ 待译，需 rights/medical/relief 逻辑函数先译。

### 数据模块逻辑函数（→ TS，非纯数据）

- **rights_data**：`get_min_wage_for_city` / `estimate_unemployment_pay`（三级fallback+括号键）/ `unemploy_duration` / `calc_injury_one_time` / `calc_injury_pension` / `get_province_injury_extra`。⬜
- **medical_data**：`estimate_inpatient`（最复杂，建议拆 getEmployeeRate+calcInpatientBase+calcBigillness）/ `_resolve_province` / `get_employee_rate`。⬜
- **relief_data**：`get_dibao_for_city`（四级fallback+子串匹配）/ `get_tekun_for_city`。⬜
- **profile.py**：`FIELD_DEFS`→类型+配置（注意 `WIZARD_STEPS` 含 lambda 不能进 JSON）/ `validate_profile` / `auto_map_tier`。⬜
- **tracking.py**：`metrics_from`（依赖 compute_current_situation）/ `render_txt`；文件 I/O 换 localStorage。⬜
- **report.py**：`build_full_report`（纯函数，注意中文关键词规则 + datetime.now）。⬜

## 数值敏感函数清单（翻译后必须逐用例严格对拍）

| 函数 | 风险点 | 基准 fixture |
|---|---|---|
| `_solve_monthly_irr` | 200次二分收敛、`(1+r)**t` 大指数浮点 | loan_methods（已dump） |
| `_monthly_payment`/`_remaining_principal` | r==0 除零保护、大指数精度 | loan_methods / affordable_debt |
| `simulate_debt_payoff` | 0.005 容差、free_pool 滚动、pick_target 排序、失控预检 | debt_payoff_* （已dump） |
| `simulate_loan_spiral` | 逐月复利、翻倍阈值、balance<0 截断 | loan_spiral（已dump） |
| `bonus_tax_compare` | 七级累进 + 速算扣除，依赖3个税表函数 | 待dump |
| `assess_debt_health` | `math.log` 求 n，monthly_pay→monthly_interest 时 log→-∞ | 待dump |
| `calc_personal_income_tax` | 累进查表，末档 Infinity 的 `<=` | tax_brackets（已dump） |

## 本阶段（试点）已译 / 已就绪

- ✅ 数据导出 `export_data.py` → `src/data/*.json`
- ✅ 黄金基准 `dump_baselines.py` → `__fixtures__/*.json`（20个：situation 10 + 子依赖4批 + 数值敏感占位6）
- ⏳ `types.ts` / `data/cost.ts` / `calc/{normalizeChildren,survival,interpretation,situation}.ts` / `index.ts` / `scripts/verify.ts`

## 交叉验证容差

| 字段 | 容差 |
|---|---|
| 金额（cost_total/social_ins/tax/income_net/surplus/cost_rows.amount） | ≤1 元 |
| tax_rate / 比率 | 0.001 |
| surplus_rate / house_saving_years | 0.1 |
| **interpretation 文本** | **严格相等** |
| breakdown / assumptions / cost_rows 结构 | deepStrictEqual |
